<?php
// DO NOT EDIT: File is generated by code generator.

namespace Pokepay\Request;

use DateTime;

class CreateCampaign extends Base
{
    protected $method = 'POST';
    public $responseClass = \Pokepay\Response\Campaign::class;
    private $name;
    private $privateMoneyId;
    private $startsAt;
    private $endsAt;
    private $priority;
    private $event;
    public function __construct($name, $privateMoneyId, $startsAt, $endsAt, $priority, $event, $optionalParams = array())
    {
        parent::__construct();
        $this->name = $name;
        $this->privateMoneyId = $privateMoneyId;
        $this->startsAt = $startsAt;
        $this->endsAt = $endsAt;
        $this->priority = $priority;
        $this->event = $event;

        $this->optionalParams = $optionalParams;
    }

    public function getPath()
    {
        return '/campaigns';
    }

    public function getParams()
    {
        $params = array(
            'name' => $this->name,
            'private_money_id' => $this->privateMoneyId,
            'starts_at' => $this->startsAt,
            'ends_at' => $this->endsAt,
            'priority' => $this->priority,
            'event' => $this->event,
        );

        $params += $this->optionalParams;

        if (isset($params['startsAt']) && $params['startsAt'] instanceof DateTime) {
            $params['startsAt'] = $params['startsAt']->format(DateTime::ATOM);
        }
        if (isset($params['endsAt']) && $params['endsAt'] instanceof DateTime) {
            $params['endsAt'] = $params['endsAt']->format(DateTime::ATOM);
        }
        if (isset($params['pointExpiresAt']) && $params['pointExpiresAt'] instanceof DateTime) {
            $params['pointExpiresAt'] = $params['pointExpiresAt']->format(DateTime::ATOM);
        }

        return $params;
    }
}
